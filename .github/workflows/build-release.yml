name: Build Release APK

on:
  push:
    tags:
      - 'v*.*.*'
      - 'v*.*.*-*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Extract version from tag
        id: get_version
        run: |
          echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          echo "VERSION_NAME=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT

      - name: Build Release APK
        run: ./gradlew assembleRelease --stacktrace

      - name: Rename APK
        run: |
          mv app/build/outputs/apk/release/app-release.apk \
             app/build/outputs/apk/release/MisLecturitas-${{ steps.get_version.outputs.VERSION_NAME }}.apk

      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ steps.get_version.outputs.VERSION }}
          release_name: Release ${{ steps.get_version.outputs.VERSION }}
          body: |
            ## 🎉 Mis Lecturitas ${{ steps.get_version.outputs.VERSION }}

            ### 📥 Descarga
            Descargá el APK desde abajo y instalalo en tu dispositivo Android.

            ### ⚙️ Instalación
            1. Descargá el APK
            2. Habilitá "Instalar apps de fuentes desconocidas" en tu celular
            3. Abrí el APK descargado
            4. Seguí las instrucciones de instalación

            ### 🆕 Novedades
            Ver los commits incluidos en esta versión.

            ---
            🤖 Compilado automáticamente por GitHub Actions
          draft: false
          prerelease: ${{ contains(steps.get_version.outputs.VERSION, '-') }}

      - name: Upload Release APK
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }}
          asset_path: app/build/outputs/apk/release/MisLecturitas-${{ steps.get_version.outputs.VERSION_NAME }}.apk
          asset_name: MisLecturitas-${{ steps.get_version.outputs.VERSION_NAME }}.apk
          asset_content_type: application/vnd.android.package-archive

      - name: Upload to Artifacts (backup)
        uses: actions/upload-artifact@v4
        with:
          name: release-apk-${{ steps.get_version.outputs.VERSION }}
          path: app/build/outputs/apk/release/MisLecturitas-${{ steps.get_version.outputs.VERSION_NAME }}.apk
          retention-days: 365
